services:
  rabbitmq:
    ports:
      - "5672:5672"
      - "15672:15672"

  users-service-migrate:
    build: .
    env_file: .env

  users-service:
    build: .
    env_file: .env
    ports:
      - "8000:8000"

  users-service_db:
    ports:
      - "5432:5432"